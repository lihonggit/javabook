package demo.appinterface.config;

/**
 * 服务配置接口
 *
 */
public interface AppServerConfig {
	public static final String RESULT_STATUS = "server_status"; // 返回状态
	public static final String RESULT_ISNULL = "server_data"; // 返回是否有值
	public static final String RESULT_MSG = "server_msg"; // 返回消息
}
